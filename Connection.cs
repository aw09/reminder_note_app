using System;

namespace reminder_notes_app
{
	public class Connection
	{
		public Connection()
		{

		}

		public void create(Object object)
        {
            /*
            var contex = new db_reminder_appEntities();
            var mhs = new Mahasiswa();
            mhs.mahasiswa_id = Convert.ToInt32(textBox_id.Text);
            mhs.mahasiswa_nama = textBox_nama.Text;
            mhs.mahasiswa_alamat = textBox_alamat.Text;
            mhs.mahasiswa_jurusan = textBox_jurusan.Text;
            mhs.mahasiswa_jeniskelamin = textBox_jeniskelamin.Text;
            contex.Mahasiswa.Add(mhs);
            contex.SaveChanges();
            display_data();
            MessageBox.Show("Simpan data berhasil!!");
            */
            var contex = new db_reminder_appEntities();
            contex.

        }
    }
}
